{
  "id": "features.platform-grid",
  "purpose": "Explain the 3 flagship AI capabilities with looping product video",
  "measuredFrom": "src/components/PlatformFeatures.jsx:1-110",
  "content": {
    "properties": {
      "heading": {
        "type": "string",
        "maxWords": 14
      },
      "subheading": {
        "type": "string",
        "maxWords": 26
      },
      "features": {
        "type": "array",
        "minItems": 3,
        "maxItems": 3,
        "items": {
          "type": "object",
          "properties": {
            "label": {
              "type": "string",
              "maxWords": 4
            },
            "title": {
              "type": "string",
              "maxWords": 12
            },
            "body": {
              "type": "string",
              "maxWords": 40
            },
            "video": {
              "type": "object",
              "properties": {
                "assetRole": {
                  "const": "product-video"
                },
                "src": {
                  "type": "string"
                }
              },
              "required": [
                "assetRole",
                "src"
              ],
              "additionalProperties": false
            }
          },
          "required": [
            "label",
            "title",
            "body",
            "video"
          ],
          "additionalProperties": false
        }
      }
    },
    "required": [
      "heading",
      "subheading",
      "features"
    ],
    "additionalProperties": false
  },
  "constraints": {
    "onePerPage": true
  },
  "motion": {
    "pattern": "loop-rive-or-video",
    "reducedMotionFallback": "first-frame-static"
  },
  "homeRouteOnly": true,
  "responsive": {
    "measuredFrom": "src/components/PlatformFeatures.jsx:1-110",
    "phone": "Feature tiles use a fixed aspect ratio (aspect-[1.41364], bare) with 30px padding on each tile.",
    "tablet": "Same per-tile aspect ratio persists (tablet:aspect-[1.41364]); the tile row lays out with a gap-8 stack instead of the bare default.",
    "desktop": "Tiles drop the fixed aspect ratio in favor of a fixed height (desktop:h-full desktop:aspect-auto) with a 65%-width media column (desktop:w-[65%] desktop:max-w-[640px]) beside a flexed text column (desktop:flex-1 desktop:justify-between)."
  }
}
